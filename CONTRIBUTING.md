# Contributing

1. Create a focused branch.
2. Run `npm install` and `npm run verify`.
3. Keep real-site URLs in `test/sites.local.json`; add deterministic fixtures for regressions.
4. Preserve credential-free validation and bounded memory/network behavior.
5. Do not add third-party artwork or code without a redistribution-compatible license and attribution record.
